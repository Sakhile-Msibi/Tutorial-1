from options import Options, attrs
import numpy
import random
class Particle:
    options=Options(n=1000,G=1.0)
    def--init--(self, (x,y),**kwargs):
        self.x=x
        self.y=y
        self.mass=mass
        self.speed=0
        self.options = Particles.options.push(kwargs)

for i in range(n):
    x=random.randint(n)
    y=random.randint(n)
